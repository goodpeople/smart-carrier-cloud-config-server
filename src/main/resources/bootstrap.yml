server:
  port: 8888

spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/redbeestudios/smart-carrier-cloud-config.git
          username: username
          password: pass
management:
  endpoints:
    web:
      base-path: /
  endpoint:
    health:
      show-details: true
